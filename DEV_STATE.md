# 开发状态

更新时间：2026-09-01

## 当前目标

完成编辑级杂志风格重构：首页采用衬线大标题、首字下沉引言、衬线数据统计与勃艮第红点睛；变更记录移入顶部菜单栏通知铃铛（计数徽章 + 最近三条预览），点击「查看全部」进入完整时间线对话框。

本轮补充 GitHub Issue / Pull Request 动态统计、近期活动项目轮播与可见的全局搜索反馈。

## 已完成

### 2026-09-01 图谱节点显示回归修复

- 恢复知识地图与项目关系图的 SVG 节点/边样式：按节点类型提供颜色变量、半透明填充、描边、搜索态和标签描边，修复节点被浏览器默认黑色实心遮住文字的问题。
- 保留现有图谱数据、交互和零构建约束，仅在 `styles.css` 增加显示层样式。
- 验证：三个 JS 文件 `node --check` 通过，Python 前端契约测试 22 项通过，`git diff --check` 通过。
- 浏览器冒烟未执行：本机缺少脚本依赖的 Chrome CDP 9333 监听端口，待有浏览器会话时补做视觉回归。

### 2026-09-01 动态项目统计与搜索体验

- `scripts/sync_github_projects.py` 新增开放 PR 分页统计，并由仓库 `open_issues_count` 扣除 PR 得到开放 Issue；失败保留未知值，避免误报为 0。
- 项目索引 summary、项目卡片和总览新增开放 Issues / PR；近期活动轮播改为按近期提交、推送时间动态排序，不再只展示 Pages 站点。
- 顶栏搜索升级为跨笔记与公开项目的即时命中反馈，增加清空按钮与 Escape 清除；搜索词会同步影响项目轮播和项目列表。
- 新增对应 Python / 前端契约测试，21 项单测全部通过；`node --check app.js`、Python 编译和 `git diff --check` 通过。

### 2026-09-01 提交口径与 Issue / PR 监测

- 提交统计改为每个仓库默认分支的提交总数，不再依赖提交作者邮箱与账号身份匹配；真实刷新后 `starline-learning-view` 显示默认分支 21 次提交、近 30 天 21 次。
- Actions 索引保存每个仓库最近开放的 Issue / PR 条目；项目区新增“开放 Issues”和“开放 PR”标签，按更新时间展示标题、仓库、标签和直达链接。
- 本轮真实索引刷新成功：35 个公开仓库、总提交 30678、近 30 天提交 270、开放 Issue 2、开放 PR 2。
- 当前 `starline-learning-view` 索引快照：默认分支 21 次提交、近 30 天 21 次、开放 Issue 0、开放 PR 0；监测标签会在其他仓库有开放条目时显示对应清单。

### 布局重构与五主题色彩系统（本轮）

- **收起目录按钮移入侧栏右上角**：侧栏头部（`sidebar-head`）桌面端显示「浏览目录」标题 + 右上角收起按钮；顶栏保留 `openSidebar` 开关（收起时显示「» 展开目录」），两处入口均可用——符合测试契约与用户直觉（VS Code/Linear 同款双入口）。
- **搜索框重设计**：升级为 999px 胶囊形（Linear/Raycast 风格），边框 + 焦点时 accent 边框 + 柔和光晕 + 轻微上浮；快捷键徽章精致化（白底 + 阴影 + 小字号）。
- **五套主题色彩系统设计**：每套建立三层文字明暗节奏——标题（视觉锚点）/ 正文（柔和）/ 弱文字（退隐）。
  - **Spectrum（深空）**：标题纯白 `#fff` + 紫色微光 `text-shadow`、正文 `#a8b2d1`、弱文字 `#6a7392`；紫色 accent 光晕按钮；深空三层递进背景（`#0a0d18`/`#10162a`/`#161d36`）。标题↔正文明度差 76 级。
  - **Atelier（浅色现代）**：标题墨蓝 `#0c1a2b`、正文 `#55677c`、弱 `#9aa8ba`；级差 76。
  - **Command（终端）**：标题亮绿白 `#e8f5ee`、正文 `#7e978e`；级差 98。
  - **Atlas（知识地图）**：标题墨绿 `#243229`、正文 `#61705f`；级差 61。
  - **Blueprint（工程蓝图）**：标题藏蓝 `#0f3562`、正文 `#4f6c94`；级差 57。
- 五主题全部无横向溢出、文字层次 `hierarchy: true` 验证通过。

### 配色焕新（此前）

- **根据用户反馈「人们更喜欢绿橙蓝浅色」切换配色方向**：Atelier 从编辑级暖调（奶油白+朱砂红）改为浅色现代科技风。
- **新配色**：浅灰蓝白底 `#f6f8fb`、科技蓝主调 `#2563eb`、深灰蓝文字 `#14202e`、纯白卡片。
- **蓝绿橙功能色分配**：学习笔记统计用绿 `#16a34a`、GitHub 项目统计用蓝 `#2563eb`、维护变更统计用橙 `#ea580c`；状态标签已发布=绿、草稿=橙；主按钮回归蓝色实心；品牌条蓝绿渐变。
- **三色克制点缀**：蓝 0.97%、绿橙 <0.05%（Hero 统计、状态标签、时间线节点），84% 浅色背景保持通透。
- 保留编辑级排版骨架（衬线大标题、首字下沉、Hero 统计、通知中心）。

### 配色精修（此前）

- **根据用户截图标注的反馈精修配色**：用户标注集中在顶部导航、Hero 与项目区，数据显示勃艮第红作为主按钮背景占比过高（导航 15%、项目区 29%），且整体米白偏灰、红色偏冷。
- **Atelier 升级为奶油纸感配色**：背景 `#faf7f1`（更通透）、强调色由冷调勃艮第红 `#b3203e` 换成暖调朱砂红 `#a62b1f`（与奶油白和谐）。
- **红色点睛化**：主按钮（Hero「查看 GitHub 项目」、顶栏「新建笔记」）改为墨黑 `#201c17`，红色仅保留时间线节点、标题强调、通知徽章与品牌条等小面积点睛；红色占比从 1.45% 降至 0.27%。
- 五主题排版性格化保留：Atelier/Atlas/Blueprint/Spectrum 衬线标题，Command 等宽终端风。

### 编辑级杂志重构（此前）

- **首页 Hero 升级为编辑级杂志头版**：64px 衬线大标题（Georgia/Noto Serif SC/Songti SC）、首字下沉引言（58px 衬线大字）、右侧竖排衬线数据统计（学习笔记/GitHub 项目/变更记录）。
- **变更记录移入顶部菜单栏通知**：铃铛图标 + 计数徽章（15 条），点击展开通知面板——显示最新摘要、最近三条预览、双区块切换（功能更新/系统说明）、「查看全部」按钮；面板支持外部点击与 Esc 关闭。
- **Atelier 升级为编辑部暖白纸感配色**：米白 `#f6f4ef` 背景、墨黑 `#1f1d1a` 文字、勃艮第红 `#b3203e` 点睛（按钮/徽章/时间线节点/标题强调）。
- **五套主题排版性格化**：Atelier/Atlas/Blueprint/Spectrum 用衬线标题，Command 保留等宽终端风；通知面板与时间线对话框全主题适配。
- **修复通知面板默认展开问题**：`.notification-menu:not([open]) > .notification-panel { display: none }` 显式隐藏（UA details 行为不可靠）。
- **修复移动端对话框**：`dialog:modal` UA max-width 覆盖，390px 下全宽无圆角。

### 设计系统 v3（此前）

- 变更记录改为「顶部公告条 + 时间线对话框」形态（已被本轮通知中心取代）。
- CSS 结构化重写：单一 token 源、7 章节、无重复选择器。
- 五主题圆角性格（24/12/32/10/28px）+ 三级柔和阴影 + 胶囊元素。

### 此前已完成（v2 基线）

- GitHub 索引采集字段增强（license、languages、createdAt 等）。
- Actions 工作流校验并发布 `data/changelog.json`。

## 关键决策

- **「通知更新放顶部菜单栏」**：铃铛 + 徽章是行业标准通知模式，比页面内公告条更符合直觉；面板提供预览，对话框承载完整时间线。
- **编辑级杂志风格**：衬线标题 + 首字下沉 + 奶油纸感是中文杂志的经典气质，与"学习笔记"主题的情感（静谧求知、编辑级精致）一致；朱砂红作为唯一点睛色，克制使用。
- **排版性格化**：衬线标题仅用于编辑气质主题（Atelier/Atlas/Blueprint/Spectrum），Command 保留等宽终端风。
- 保持零构建与全部测试契约。

## 核心文件

- `index.html`：顶部菜单栏通知（notification-menu）+ 编辑级 Hero（hero-stats）+ 时间线对话框。
- `styles.css`：设计系统 v3 + 编辑级排版（衬线/首字下沉/暖白纸感）+ 通知面板。
- `app.js`：通知菜单开合（外部点击/Esc）、徽章计数、最近三条预览、Hero 统计渲染。
- `data/changelog.json`：变更记录（16 条：11 功能更新 + 5 系统说明）。
- `tests/test_frontend_contract.py`：19 项测试全部通过。

## 验证

- `node --check` 三个 JS 文件：通过。
- `python -m unittest`：19 项全部通过。
- 浏览器烟测（Edge 无头）：全部通过。
- 五主题桌面（1440px）+ 移动端（390px）：无溢出、通知面板默认收起、徽章计数正确、衬线/等宽性格正确、首字下沉 58px、对话框开关正常。
- 截图存于 `D:\Temp\v4-*.png` / `v5-atelier-hero.png`。

## 已知边界与下一步

- 示例笔记 URL 仍指向 GitHub Pages 根地址，需替换后做线上链接健康检查。
- Spectrum 玻璃拟态在低端 GPU 上可能降帧率；`prefers-reduced-motion` 自动关闭入场动画。
- 变更记录的 `commit` 字段为手动维护，需在每次提交前确认同步。
- 后续可考虑：通知面板「标记已读」、Hero 加入编辑级装饰线（pull quote 样式）、项目卡片 Bento 网格变体。
- 已完成一次真实 GitHub 索引刷新并写入 `data/github-projects.json`；后续由 Pages Actions 按 6 小时计划继续更新。
